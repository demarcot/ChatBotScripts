from src.script_utils import log, send_message, send_whisper

def health(parent, data, params):
    log(parent, "Dispatching health()")
    return

def sit(parent, data, params):
    log(parent, "Dispatching sit()")
    return

def stand(parent, data, params):
    log(parent, "Dispatching stand()")
    return

def peek(parent, data, params):
    log(parent, "Dispatching peek()")
    return

def flipTable(parent, data, params):
    log(parent, "Dispatching flipTable()")
    return

def openTable(parent, data, params):
    log(parent, "Dispatching openTable()")
    return

def bet(parent, data, params):
    log(parent, "Dispatching bet()")
    return

def check(parent, data, params):
    log(parent, "Dispatching check()")
    return

def call(parent, data, params):
    log(parent, "Dispatching call()")
    return

def fold(parent, data, params):
    log(parent, "Dispatching fold()")
    return

def allIn(parent, data, params):
    log(parent, "Dispatching allIn()")
    return

def setBlinds(parent, data, params):
    log(parent, "Dispatching setBlinds()")
    return

def kick(parent, data, params):
    log(parent, "Dispatching kick()")
    return